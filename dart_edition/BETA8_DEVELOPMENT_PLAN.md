# Monogatari Assistant Beta 8 開發計畫

> 文件狀態：Draft  
> 適用範圍：Flutter 版本（`dart_edition/`）  
> 目前專案格式版本：`1.06`  
> 建議 Beta 8 專案格式版本：`1.07`  

## 1. 版本定位

Beta 8 的核心目標不是同時完成所有進階功能，而是建立一套可被大綱、時間軸、角色狀態、關係圖、同步與 AI/MCP 共用的結構化故事資料骨架。

版本主題：

> 建立「故事事件－時間－角色狀態－角色關係」的共同資料基礎，並以最小可用功能驗證其可行性。

### 1.1 成功條件

Beta 8 完成時，使用者應能：

1. 使用群組整理既有三層大綱。
2. 將大綱場景關聯至故事時間點。
3. 在時間軸上查看與編輯大綱事件。
4. 使用簡化後的角色屬性表。
5. 查看角色在指定故事時間點的狀態。
6. 建立基本角色關係並以關係圖呈現。
7. 從大綱預覽並套用規則式整理結果。
8. 透過同步 UI 查看同步狀態，並以早期 API 完成單一帳號、兩裝置的基本同步流程。
9. 安全開啟 Beta 8 以前的專案，且不遺失原有資料。
10. 產出註解、多語系與 MCP 的技術驗證結果，作為後續版本決策依據。

### 1.2 非目標

下列項目不列入 Beta 8 正式交付範圍：

- 多人即時共同編輯。
- 複雜的同步衝突自動合併。
- 完整協作權限、通知與使用者提及。
- 關係圖的進階動畫、巨量節點最佳化與自由排版系統。
- 任意曆法換算或完整世界觀曆法編輯器。
- AI 在未經確認下直接修改專案資料。
- MCP 的高風險寫入工具。
- 完整中、日、英翻譯。

## 2. 優先級與交付範圍

| 優先級 | 項目 | Beta 8 建議範圍 | 交付要求 |
| --- | --- | --- | --- |
| P0 | 大綱群組 | 群組 CRUD、排序、收合、持久化 | 必須完成 |
| P0 | 角色屬性簡化 | 新角色資料模型、簡化表單、舊資料遷移 | 必須完成 |
| P0 | 即時狀態 | 角色「當前／指定時間點狀態」的新增與編輯 | 必須完成 |
| P0 | 時間軸骨架 | 時間點、事件、排序、篩選、與大綱關聯 | 必須完成 |
| P1 | 關係圖骨架 | 角色節點、關係邊、基本圖形檢視與編輯 | 最小可用版本 |
| P1 | 屬性快照 | 由基礎資料加變更事件推導指定時間點狀態 | 最小可用版本 |
| P1 | 大綱自動整理 | 規則式分析、差異預覽、使用者確認後套用 | 最小可用版本 |
| P1 | 同步 UI/API | 狀態 UI、手動同步、基本上下載、衝突保留 | 最小可用版本 |
| P2 | 註解 | 資料錨點、註解模型與 UI 原型 | 設計與技術驗證 |
| P2 | 多語系 | 字串盤點、localization 架構與代表頁面 PoC | 設計與技術驗證 |
| P2 | MCP | 唯讀資源／工具 PoC、安全與版本策略 | 設計與技術驗證 |

## 3. 共同架構前置工作

P0 與 P1 功能共用相同的資料識別、版本、排序與遷移基礎。若未先完成此階段，後續容易出現角色改名後關聯失效、同步無法判斷同一筆資料，以及時間快照指向錯誤對象等問題。

### 3.1 穩定識別碼

#### 現況

- 大綱的故事線、事件與場景已有字串 ID。
- 大綱 ID 目前以微秒時間戳產生。
- 角色資料目前為 `Map<String, CharacterEntryData>`，Map key 主要使用角色名稱。
- `uuid` 套件已存在於專案依賴中。

#### 計畫

- 新資料一律使用 UUID v4。
- 為角色新增不可變的 `characterId`。
- 角色名稱改為可編輯欄位，不再作為主要識別碼。
- 所有跨模組關聯只保存 ID，不保存名稱作為唯一索引。
- 顯示名稱在讀取時由 ID 查詢，避免角色改名造成關聯失效。
- 保留舊大綱 ID，不強制重建，避免破壞既有引用。

#### 驗收標準

- 角色改名後，大綱、時間軸、狀態與關係資料仍指向同一角色。
- 快速建立多筆資料不會產生重複 ID。
- 儲存並重新開啟後，所有 ID 保持不變。

### 3.2 專案格式版本與遷移

建議將專案格式由 `1.06` 升至 `1.07`，新增明確的遷移流程，而不是讓各 Codec 分散猜測舊格式。

建議流程：

```text
讀取 XML
  → 判斷專案格式版本
  → 解析舊格式
  → 執行 1.06 → 1.07 遷移
  → 驗證引用完整性
  → 建立目前版本 ProjectData
  → 使用者儲存時寫回 1.07
```

#### 遷移規則

- 舊角色 Map key 轉為 `displayName`，並為每名角色產生 UUID。
- 原有角色欄位完整保留；無法直接分類的欄位進入相容區或自訂屬性。
- 大綱中的人物名稱盡可能對應到角色 ID。
- 同名角色或找不到對應角色時保留原始文字，並記錄遷移警告。
- 舊場景 `time` 字串保留，同時嘗試轉換成新時間點資料。
- 新增模組缺失時使用空集合，不因缺少新節點而拒絕載入舊專案。
- 遷移只在記憶體中進行；未經使用者儲存，不覆寫原檔。

#### 驗收標準

- `1.06` 測試檔可正常開啟、編輯並另存為 `1.07`。
- 無法自動對應的資料不會被丟棄。
- 載入較新且不支援的格式時，沿用既有版本警告策略。
- XML 特殊字元、空值、缺失節點與重複名稱均有測試。

### 3.3 建議的共用資料概念

以下是概念模型，實作時可依現有 Freezed 與 Riverpod 架構拆分檔案：

```dart
StoryTimePoint {
  id;
  label;
  order;
  displayTime;
  calendarId?;
  sortableValue?;
}

TimelineEvent {
  id;
  title;
  timePointId;
  sourceType;       // manual, outline, characterState
  sourceId?;
  characterIds;
  locationIds;
  description;
}

CharacterProfile {
  characterId;
  displayName;
  aliases;
  coreAttributes;
  customAttributes;
}

CharacterStateChange {
  id;
  characterId;
  timePointId;
  changes;
  note;
}

CharacterRelation {
  id;
  sourceCharacterId;
  targetCharacterId;
  relationType;
  label;
  startTimePointId?;
  endTimePointId?;
  note;
}
```

### 3.4 時間排序策略

小說時間不一定能以真實日期表示，因此 Beta 8 不應只使用 Dart `DateTime`。

每個時間點至少包含：

- `order`：必要，提供穩定排序。
- `label`：必要，例如「第一章開頭」、「戰爭後三日」。
- `displayTime`：選填，保存作者輸入的故事時間文字。
- `sortableValue`：選填，能解析為日期或數字時用於輔助排序。

當故事時間不明確時，以手動 `order` 為準。

## 4. P0 詳細開發計畫

### 4.1 大綱群組

#### 功能範圍

- 建立、重新命名、刪除群組。
- 群組排序與收合。
- 將既有故事線移入或移出群組。
- 提供「未分組」區域。
- 群組資料寫入 XML。
- 刪除非空群組時，由使用者選擇保留內容或一併刪除。

#### 建議資料

```dart
OutlineGroupData {
  groupId;
  name;
  description;
  order;
  isCollapsed;
  storylineIds;
}
```

`isCollapsed` 若被視為個人 UI 偏好，可存入本機設定而非專案檔；Beta 8 需在實作前決定是否跨裝置同步。

#### 工作拆解

1. 建立 `OutlineGroupData` 與 provider。
2. 擴充 `ProjectData` 與 snapshot/deep-copy 流程。
3. 擴充 Outline XML Codec。
4. 建立群組清單與未分組區域。
5. 完成群組內、群組間排序。
6. 加入刪除確認與復原／歷史紀錄。
7. 補齊 dirty state、自動儲存與專案歷史整合。
8. 建立模型、Codec、provider 與 widget 測試。

#### 驗收標準

- 關閉並重開專案後，群組與排序保持一致。
- 故事線在任一時間只屬於一個群組。
- 刪除群組不會在未確認時刪除故事線。
- 群組操作能被 undo/redo 或既有專案歷史機制正確記錄。

### 4.2 角色屬性表簡化

#### 設計原則

- 常用欄位優先，低頻欄位收進進階或自訂區域。
- 固定角色資料與會隨劇情改變的狀態分離。
- 不再使用角色名稱作為資料主鍵。
- 舊欄位必須可遷移、可顯示、可再次儲存。

#### 建議分區

| 分區 | 代表欄位 | 時間性 |
| --- | --- | --- |
| 基本資料 | 姓名、別名、生日、性別、身份 | 通常固定 |
| 外觀摘要 | 身高、髮色、眼睛、服裝摘要 | 可固定或覆寫 |
| 性格與動機 | 性格、信念、目標、恐懼 | 可固定或覆寫 |
| 自訂屬性 | 使用者自訂 key/value | 由使用者決定 |
| 故事狀態 | 地點、健康、陣營、情緒、持有物 | 隨時間變化 |
| 備註 | 自由文字 | 不進行推導 |

#### 工作拆解

1. 盤點 `CharacterDataKeys` 與現有陣列／滑桿欄位。
2. 定義新 `CharacterProfile` 與自訂屬性格式。
3. 定義舊欄位至新欄位的遷移表。
4. 將角色 provider 從名稱索引轉為 ID 索引。
5. 更新新增、改名、刪除與選取流程。
6. 重做角色表單的基本／進階／狀態分區。
7. 擴充 Character XML Codec。
8. 補齊搜尋、匯出 Markdown、專案快照與 dirty state。
9. 建立遷移與 UI 回歸測試。

#### 驗收標準

- 舊專案中的所有角色欄位均可找到或由相容區存取。
- 角色改名不會建立重複角色或遺失資料。
- 使用者可在簡化畫面完成建立角色所需的核心操作。
- 未填寫的進階欄位不造成畫面噪音。

### 4.3 角色即時狀態

Beta 8 UI 建議使用「目前狀態」或「指定時間點狀態」，避免與網路即時更新混淆。

#### 最小狀態欄位

- 所在地。
- 健康／生死狀態。
- 情緒。
- 所屬陣營。
- 持有物。
- 自訂狀態。
- 補充說明。

#### 行為規則

- 未選時間點時顯示故事最新狀態。
- 選取時間點時，顯示截至該時間點累積出的狀態。
- 編輯歷史時間點時，新增狀態變更事件，而不是覆寫角色基礎資料。
- UI 必須清楚標示目前查看的時間點。
- 提供「跳至最新狀態」操作。

#### 工作拆解

1. 建立 `CharacterStateChange` 模型。
2. 建立依時間點計算狀態的純函式／use case。
3. 建立全域時間游標 provider。
4. 在角色頁加入狀態卡與時間點提示。
5. 建立新增、修改、刪除狀態變更流程。
6. 串接時間軸事件顯示。
7. 擴充 XML Codec 與 ProjectData。
8. 建立狀態推導、排序與邊界測試。

#### 驗收標準

- 切換時間點時，角色狀態可穩定重建。
- 刪除某次變更後，後續狀態會重新計算。
- 同一時間點有多次變更時採用穩定順序。
- 基礎資料與劇情狀態不會互相覆寫。

### 4.4 時間軸骨架

#### 功能範圍

- 建立、編輯、刪除與排序時間點。
- 建立手動時間軸事件。
- 將大綱故事線、事件或場景連結至時間點。
- 依角色、事件來源與文字搜尋篩選。
- 點擊時間軸事件可回到來源大綱。
- 切換全域故事時間游標。

#### 初期 UI

- 先採垂直列表或分組列表，確保桌面與行動裝置皆可使用。
- 不把複雜圖形時間軸列為 P0。
- 清楚區分手動事件、大綱事件與角色狀態事件。

#### 工作拆解

1. 建立 `StoryTimePoint` 與 `TimelineEvent`。
2. 建立時間點排序與重新編號策略。
3. 建立時間軸 providers/selectors。
4. 建立時間軸頁面、事件卡與篩選列。
5. 在大綱場景編輯器加入時間點選擇。
6. 建立來源導航。
7. 串接角色狀態時間游標。
8. 擴充 ProjectData、XML 與歷史快照。
9. 建立模型、排序、引用完整性與 UI 測試。

#### 驗收標準

- 無真實日期的事件仍可被穩定排序。
- 大綱項目移動或改名後，時間軸引用仍有效。
- 刪除被引用的時間點時會警告並提供處理方式。
- 500 筆事件的基本捲動與篩選不出現明顯卡頓。

## 5. P1 詳細開發計畫

### 5.1 角色關係圖骨架

#### 最小可用範圍

- 以角色資料建立節點。
- 建立有方向或無方向的關係。
- 設定關係類型、顯示標籤、備註與有效時間範圍。
- 點擊節點開啟角色摘要。
- 點擊關係邊開啟編輯器。
- 提供基本自動排版、縮放與平移。

#### 資料規則

- 關係端點一律引用 `characterId`。
- 同一對角色可有多筆不同類型關係。
- 支援單向關係，例如「A 暗戀 B」。
- 刪除角色前提示其關係數量。
- 時間游標改變時，只顯示當下有效的關係。

#### 驗收標準

- 角色改名不影響關係。
- 孤立節點也能正常顯示。
- 關係可保存並重新載入。
- 50 個角色、100 條關係可正常操作。
- 行動裝置至少可檢視與選取，不要求完整桌面編輯體驗。

### 5.2 角色屬性快照

#### 實作策略

不為每個時間點保存完整角色複本，而是保存變更事件：

```text
角色基礎資料
  + 時間點 A 的變更
  + 時間點 B 的變更
  = 時間點 B 的角色快照
```

#### 最小可用範圍

- 先支援 P0 定義的故事狀態欄位。
- 提供目前時間點與前一時間點的差異。
- 能查看變更來源與備註。
- 支援還原或刪除單次狀態變更。

#### 快取策略

- 初期以純函式重建為正確性基準。
- 若大型專案出現效能問題，再加入以角色 ID、時間點 ID、資料 revision 為 key 的記憶體快取。
- 任何狀態變更、時間排序變更或角色基礎資料變更都必須使相關快取失效。

#### 驗收標準

- 相同輸入必須產生相同快照。
- 儲存前後推導結果一致。
- 修改早期狀態後，所有後續時間點正確更新。
- 快照不會被當成新的可獨立編輯角色資料。

### 5.3 大綱自動添加與整理

#### Beta 8 採用規則式流程

1. 掃描大綱中尚未關聯時間軸的項目。
2. 找出可對應的角色名稱、場景時間與地點。
3. 產生新增、更新、忽略與警告清單。
4. 顯示差異預覽。
5. 使用者逐項或全部確認。
6. 以單一交易／歷史節點套用，允許一次復原。

#### 規則優先順序

- 明確的角色 ID／時間點 ID 關聯優先。
- 完全相同名稱次之。
- 別名匹配只能提出建議，不直接套用。
- 模糊匹配與 AI 分析不得自動寫入。
- 無法確認的資料保留原始文字。

#### 驗收標準

- 預覽階段不改變 ProjectData 與 dirty state。
- 同一份資料重複執行不會建立重複事件。
- 套用結果可一次 undo。
- 每項變更都能追溯至來源大綱 ID。

### 5.4 同步 UI/API

同步是 P1 中風險最高的項目，Beta 8 應以可驗證的垂直切片為目標。

#### 最小可用情境

> 同一使用者在裝置 A 上傳一個專案，裝置 B 下載；兩端先後修改時，可辨識版本差異，且不會靜默覆蓋資料。

#### 同步 UI

- 未登入／未設定。
- 尚未同步。
- 同步中。
- 已同步與最後同步時間。
- 本機有未上傳變更。
- 遠端有新版本。
- 發生衝突。
- 同步失敗與重試。
- 手動「立即同步」。

#### API 初稿

```text
POST   /v1/projects
GET    /v1/projects
GET    /v1/projects/{projectId}
PUT    /v1/projects/{projectId}
POST   /v1/projects/{projectId}/sync
GET    /v1/projects/{projectId}/revisions
```

每次同步至少攜帶：

- `projectId`
- `deviceId`
- `baseRevision`
- `clientRevision`
- `schemaVersion`
- `updatedAt`
- 內容雜湊
- 專案內容或變更集合

#### Beta 8 衝突策略

- 採 optimistic concurrency control。
- `baseRevision` 不等於遠端目前 revision 時，不直接覆寫。
- 先保存本機與遠端兩個版本。
- UI 顯示衝突並允許選擇：
  - 保留本機為新版本。
  - 採用遠端並備份本機。
  - 匯出兩份檔案後稍後處理。
- 欄位級自動合併留待後續版本。

#### 安全要求

- 驗證資料傳輸使用 TLS。
- Token 不寫入專案 XML。
- Token 使用平台安全儲存方案；PoC 若尚未導入，禁止正式發佈登入功能。
- API 不記錄專案正文、角色資料或 token 至一般 log。
- 明確限制檔案大小、請求頻率與可接受 schema version。

#### 驗收標準

- 正常上傳、下載、重試與離線狀況皆有測試。
- 衝突不會造成任一版本靜默遺失。
- 同步失敗不影響本機開啟與儲存。
- API mock、契約測試與最小整合測試可在 CI 執行。
- 若後端未達安全與可靠性標準，Beta 8 UI 必須保持 feature flag 關閉。

## 6. P2 設計與技術驗證

P2 的完成定義是「有文件、有原型、有測試結論與下一步估算」，不是進入正式產品流程。

### 6.1 註解與提及

#### 要驗證的問題

- 註解應錨定章節、文字範圍、角色、大綱事件，還是所有實體？
- 章節文字被修改後，文字範圍錨點如何重新定位？
- 「提及」是引用角色／設定，還是通知其他使用者？
- 註解是否需要待辦、已解決、作者與時間資訊？

#### 建議 PoC

- 建立通用 `EntityReference`：`entityType + entityId`。
- 對角色或大綱事件新增單人註解。
- 對章節文字範圍測試 offset、上下文片段與內容雜湊三種錨定方式。
- `@角色` 僅作為內部實體引用；多人通知留待帳號與協作系統完成。

#### 交付物

- 資料模型決策記錄。
- 章節錨點 PoC。
- 文字異動後錨點成功率測試。
- 多人協作前置需求清單。

### 6.2 多語系

#### 要驗證的問題

- 現有 UI 硬編碼字串數量與分布。
- 是否採 Flutter 官方 `gen_l10n`。
- 專案內容語言與 UI 語言是否完全分離。
- 中、日、英文字長及複數規則對版面的影響。

#### 建議 PoC

- 導入 localization 骨架。
- 選擇設定頁或角色簡化頁作為代表頁面。
- 建立繁體中文與英文資源。
- 驗證執行期切換、fallback、日期與數字格式。

#### 交付物

- 字串盤點報告。
- key 命名與模組拆分規範。
- 代表頁面 PoC。
- 全專案遷移工作量估算。

### 6.3 AI/MCP

#### Beta 8 原則

- 先唯讀、後寫入。
- 工具使用穩定 ID，不依賴顯示名稱。
- 回傳結構化資料與 schema version。
- 大型正文採分頁、篩選或摘要，避免一次傳出完整專案。
- 寫入工具必須具備預覽、確認、稽核與復原機制；不列入 Beta 8 PoC。

#### 建議唯讀工具

```text
get_project_summary
list_outline_groups
list_outline_events
get_timeline
get_character
get_character_state_at_time
list_character_relations
search_project_entities
```

#### 要驗證的問題

- MCP server 與 Flutter app 採行程內、localhost 或獨立服務何者較合適。
- 專案未開啟、已修改但未儲存時，工具應讀取哪一份狀態。
- 使用者如何授權可見的專案與欄位。
- 如何避免透過工具意外洩漏未選取的作品內容。

#### 交付物

- MCP tool/resource schema 草案。
- 至少兩個唯讀工具 PoC。
- 權限與威脅模型。
- 寫入工具的確認／復原流程設計。

## 7. 建議實作階段

階段以依賴關係排序；是否對應一個或多個 sprint，可依團隊人力調整。

| 階段 | 內容 | 主要產出 | 進入下一階段條件 |
| --- | --- | --- | --- |
| Phase 0 | 基準、feature flags、測試樣本 | 效能與相容性基準 | 現有測試全綠 |
| Phase 1 | UUID、ProjectData、1.07 遷移 | 共用資料骨架 | 舊檔可無損載入 |
| Phase 2 | 角色屬性簡化 | 新角色模型與 UI | 改名及存取穩定 |
| Phase 3 | 大綱群組 | 群組 UI、Codec、排序 | 儲存與歷史整合完成 |
| Phase 4 | 時間點與時間軸 | 時間軸頁、大綱關聯 | 無日期事件可穩定排序 |
| Phase 5 | 角色狀態與快照 | 狀態變更及推導 | 指定時間點結果正確 |
| Phase 6 | 關係圖 | 關係模型與基本圖形 UI | 時間篩選及持久化完成 |
| Phase 7 | 大綱自動整理 | 規則引擎與差異預覽 | 重複執行具冪等性 |
| Phase 8 | 同步垂直切片 | UI、API 契約、衝突保留 | 不會靜默遺失版本 |
| Phase 9 | 註解／i18n／MCP PoC | 三份決策與原型結果 | 可對後續版本估算 |
| Phase 10 | 穩定化與發布 | 回歸、效能、遷移驗證 | Beta 8 DoD 全部通過 |

### 7.1 Phase 0：基準與防護

- 建立至少三種匿名化測試專案：
  - `1.06` 小型專案。
  - 大量章節、角色與大綱項目的壓力專案。
  - 包含空值、重複名稱與特殊字元的邊界專案。
- 記錄開啟、儲存、大綱頁與角色頁的基準耗時。
- 為同步、關係圖與新時間軸加入 feature flags。
- 確認 `flutter analyze`、完整 `flutter test` 為綠燈。

### 7.2 Phase 1：資料骨架

- 建立新模型與專案格式節點。
- 將角色識別改為 UUID。
- 建立引用驗證器，回報 dangling references。
- 實作 `1.06 → 1.07` 遷移。
- 擴充 project snapshot、history、dirty state 與 XML parser tests。

### 7.3 Phase 2－8：垂直功能切片

每個功能均採以下順序：

1. 模型與純函式。
2. Codec 與遷移。
3. Provider／use case。
4. 最小 UI。
5. 歷史、dirty state、自動儲存。
6. 單元與 widget 測試。
7. 效能與平台驗證。

避免先建立完整 UI，最後才補持久化與遷移。

### 7.4 Phase 10：穩定化

- 凍結 XML schema。
- 完成所有舊檔遷移測試。
- 執行 Windows、macOS、Linux、Android、iOS 代表性測試。
- 檢查鍵盤、滑鼠、觸控與小螢幕操作。
- 檢查自動儲存、備份、復原與當機後資料完整性。
- 完成 release notes、已知問題與回復方案。

## 8. 測試策略

### 8.1 單元測試

- UUID 與引用完整性。
- 時間排序與同時間點穩定順序。
- 角色狀態推導與快照差異。
- 大綱整理規則與冪等性。
- 同步 revision 與衝突判定。
- 各版本 migration。

### 8.2 Codec／round-trip 測試

每個新增模型都必須驗證：

```text
Model → XML → Model
```

並比較：

- ID。
- 排序。
- 所有跨模組引用。
- Unicode 與 XML 特殊字元。
- 空值與選填欄位。

### 8.3 Provider 與歷史測試

- 修改後 dirty state 必須更新。
- 儲存成功後 dirty state 必須清除。
- undo/redo 不得產生懸空引用。
- 切換專案時不得殘留前一專案時間游標或選取狀態。

### 8.4 Widget／整合測試

- 大綱群組 CRUD 與排序。
- 角色改名及切換。
- 時間軸建立、篩選與來源導航。
- 指定時間點的角色狀態。
- 關係圖節點與邊選取。
- 自動整理預覽與確認。
- 同步狀態及衝突畫面。

### 8.5 效能門檻

建議測試資料：

- 100 名角色。
- 500 個時間點。
- 2,000 個時間軸事件。
- 300 條角色關係。
- 1,000 次角色狀態變更。

最低要求：

- 篩選與時間游標切換不阻塞 UI 數秒。
- 大型清單採 selector、分段計算或虛擬化，避免整頁不必要 rebuild。
- 狀態快照若超出目標耗時，再導入可失效快取。

## 9. 風險與對策

| 風險 | 等級 | 影響 | 對策 |
| --- | --- | --- | --- |
| 角色以名稱作為 key | 高 | 改名後跨模組引用失效 | Phase 1 改為 UUID |
| XML schema 同時大量變動 | 高 | 舊檔遷移與資料遺失 | 集中 migration、round-trip 測試、延後 schema freeze |
| 時間模型過度依賴日期 | 高 | 架空世界與相對時間無法表示 | 以 order 為必要欄位，日期為選填 |
| 同步靜默覆蓋 | 極高 | 使用者作品遺失 | revision 檢查、雙版本保留、feature flag |
| 快照保存完整複本 | 高 | 重複資料與不一致 | 保存變更事件，執行時推導 |
| 關係圖套件跨平台問題 | 中 | 桌面正常、行動裝置不可用 | 先做技術 spike，再固定套件 |
| 自動整理誤判 | 中 | 產生錯誤資料 | 差異預覽、確認後套用、單次 undo |
| 大量 provider rebuild | 中 | 時間軸與角色頁卡頓 | 細粒度 selector、純函式、基準測試 |
| P2 擴張成正式功能 | 中 | 排擠 P0/P1 | 固定 PoC 交付定義與 timebox |

## 10. 發布與回復策略

### 10.1 Feature flags

以下功能建議可獨立關閉：

- 新時間軸 UI。
- 關係圖。
- 自動整理。
- 雲端同步。
- MCP PoC。

資料格式應能容忍 UI 功能被關閉；關閉功能不可刪除既有資料。

### 10.2 備份

- 第一次將 `1.06` 專案儲存為 `1.07` 前建立備份。
- 同步衝突時建立本機副本。
- 遷移錯誤時保持原檔不變，並允許輸出診斷資訊。

### 10.3 Beta 發布節奏

建議先發布內部／測試者版本：

1. 僅開啟 P0。
2. 收集舊檔遷移與時間軸回饋。
3. 逐項開啟關係圖、自動整理。
4. 最後邀請少量使用者測試同步。
5. P2 PoC 不出現在一般使用者介面，除非有明確的開發者開關。

## 11. Definition of Done

Beta 8 只有在以下條件全部滿足時才視為完成：

- [ ] P0 功能全部通過驗收標準。
- [ ] P1 各功能至少完成定義中的最小可用版本。
- [ ] P2 各項均有設計文件、PoC 結果與後續估算。
- [ ] `1.06 → 1.07` 遷移測試通過。
- [ ] 舊專案未出現已知資料遺失。
- [ ] 所有新增資料均可 XML round-trip。
- [ ] 所有跨模組引用使用穩定 ID。
- [ ] 新功能已整合 dirty state、儲存、自動備份與歷史紀錄。
- [ ] `flutter analyze` 無新增問題。
- [ ] 完整 `flutter test` 通過。
- [ ] 代表性桌面與行動平台完成 smoke test。
- [ ] 同步衝突不會靜默覆蓋任一版本。
- [ ] release notes、已知問題與備份／回復說明完成。

## 12. 建議 Issue／Epic 拆分

```text
Epic B8-00：Project schema 1.07 與共用 ID
  B8-001 角色 UUID 與引用模型
  B8-002 ProjectData 新節點
  B8-003 1.06 → 1.07 migration
  B8-004 引用完整性檢查器
  B8-005 XML round-trip 測試集

Epic B8-10：大綱群組
  B8-101 OutlineGroupData 與 Codec
  B8-102 群組 CRUD
  B8-103 群組內／跨群組排序
  B8-104 歷史、dirty state 與 widget tests

Epic B8-20：角色資料簡化
  B8-201 新 CharacterProfile
  B8-202 舊角色欄位遷移
  B8-203 角色簡化表單
  B8-204 搜尋、匯出與回歸測試

Epic B8-30：時間軸與角色狀態
  B8-301 StoryTimePoint
  B8-302 TimelineEvent
  B8-303 時間軸頁與篩選
  B8-304 大綱時間關聯
  B8-305 CharacterStateChange
  B8-306 指定時間點狀態推導
  B8-307 快照差異與效能

Epic B8-40：角色關係圖
  B8-401 CharacterRelation 與 Codec
  B8-402 關係 CRUD
  B8-403 圖形檢視與基本排版
  B8-404 時間篩選與跨平台測試

Epic B8-50：大綱自動整理
  B8-501 掃描與匹配規則
  B8-502 差異預覽
  B8-503 交易式套用與 undo
  B8-504 冪等與誤判測試

Epic B8-60：同步 UI/API
  B8-601 API 契約與 mock
  B8-602 同步狀態機
  B8-603 同步狀態 UI
  B8-604 revision 與衝突保留
  B8-605 安全、離線與整合測試

Epic B8-70：技術驗證
  B8-701 註解錨點 PoC
  B8-702 多語系 PoC
  B8-703 MCP 唯讀工具 PoC

Epic B8-80：穩定化與發布
  B8-801 大型專案效能
  B8-802 跨平台 smoke tests
  B8-803 遷移與備份驗證
  B8-804 Release notes 與已知問題
```

## 13. 開發決策檢查點

以下決策應在對應階段開始前確認，避免開發中反覆改動：

1. 大綱群組是否只允許單層。
2. 群組收合狀態屬於專案資料或本機偏好。
3. 角色自訂屬性的型別是否僅支援文字，或包含數字、布林與選項。
4. 時間點是否需要跨故事線共用。
5. 同一時間點內事件的次序規則。
6. 關係是否支援雙向顯示但以兩筆單向資料保存。
7. 同步後端、帳號與安全儲存方案。
8. P2 PoC 的 timebox 與停止條件。

在上述決策完成前，可先進行不受選項影響的模型測試、遷移樣本與 UI wireframe，但不應凍結 XML schema。
